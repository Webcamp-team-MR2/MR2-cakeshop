<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Document</title>
    </head>
    <body>
        <%= link_to "商品新規登録", new_admin_item_path %>
        <h3>商品一覧画面</h3>
        <table class = "table table-fover">
            <tr>
                <th>ID</th>
                <th>画像</th>
                <th>商品名</th>
                <th>税抜価格</th>
                <th>カテゴリー</th>
                <th>説明文</th>
                <th>販売ステータス</th>
                <th></th>
            </tr>
            <% @items.each do |item| %>
                <tr>
                    <td><%= item.id%>
                    <td><%= attachment_image_tag item,:image,:fill,100,100,format: 'jpeg'%></td>
                    <td><%= item.name%></td>
                    <td><%= item.price.to_s + "円" %></td>
                    <td><%= item.category.sweets_name%></td>
                    <td><%= item.description%></td>
                    <td><% if item.sale_status == true %>
                        <%= "販売中" %>
                        <% else %>
                            <%= "売り切れ" %>
                        <% end %>
                    </td>
                    <td><%= link_to "商品編集", edit_admin_item_path(item) %></td>
                </tr>
            <% end %>
        </table>
        <%= paginate @items %>
    </body>
</html>

